# Node manifest fixture

`package.json` can be updated. JavaScript semantic repair is unavailable, so a successful manifest edit still needs review.

```bash
evolvectl upgrade node:left-pad --to 1.3.1 --workspace examples/node-pnpm-command-adapter
```
