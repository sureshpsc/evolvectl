# External adapter

`evolvectl-adapter-*` executables speak a JSON handshake on stdin/stdout. This example implements handshake only. It does not claim semantic repair.

```bash
go run ./examples/custom-tool-plugin
```
