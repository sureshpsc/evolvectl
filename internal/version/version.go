// Package version holds the CLI release identity.
package version

const (
	// Version is the evolvectl product version.
	Version = "0.1.0"
	// Protocol is the executable adapter protocol version.
	Protocol = "1.0"
	// ReportSchema is the persisted run report schema.
	ReportSchema = "evolvectl.run/v1"
)
