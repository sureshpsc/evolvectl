# Security

Report undisclosed vulnerabilities through a private GitHub security advisory. Do not open a public issue for an unfixed vulnerability.

Evolvectl keeps source local. It does not upload a repository unless you explicitly configure a network integration. Recipes cannot run shell commands. The local UI binds to loopback and is read-only. Reports redact token-shaped strings; that is not a complete secret scanner.
