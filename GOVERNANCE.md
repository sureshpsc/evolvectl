# Governance

The canonical repository accepts changes through pull requests. Maintainers review and merge. `main` should be protected, with required CI, and without force-push.

Go and Python adapters are reference implementations. A command adapter or `evolvectl-adapter-*` executable can integrate another tool without changing campaign orchestration.
